server:
  port: 8082

spring:
  application:
    name: my-second-service
  cloud:
    compatibility-verifier:
      enabled: false

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

